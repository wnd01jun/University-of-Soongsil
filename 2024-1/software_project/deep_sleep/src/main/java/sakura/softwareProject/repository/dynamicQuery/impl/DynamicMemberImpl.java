package sakura.softwareProject.repository.dynamicQuery.impl;

import sakura.softwareProject.repository.dynamicQuery.DynamicMemberRepository;

public class DynamicMemberImpl implements DynamicMemberRepository {
//    private final JPAQueryFactory queryFactory;

//    public DynamicMemberRepositoryImpl(EntityManager em) {
//        queryFactory = new JPAQueryFactory(em);
//    }
}
